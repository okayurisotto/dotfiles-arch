<?xml version='1.0'?>
<!DOCTYPE fontconfig SYSTEM 'fonts.dtd'>
<fontconfig>
  <dir>~/.fonts</dir>

  <!-- Kakuyomu {{{ -->

  <alias>
    <family>YuMincho</family>
    <prefer>
      <family>Noto Serif CJK JP</family>
    </prefer>
    <default>
      <family>fixed</family>
    </default>
  </alias>

  <!-- }}} -->
</fontconfig>
